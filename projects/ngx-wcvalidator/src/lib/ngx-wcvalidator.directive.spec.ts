import { NgxWcvalidatorDirective } from './ngx-wcvalidator.directive';

describe('NgxWcvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxWcvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
